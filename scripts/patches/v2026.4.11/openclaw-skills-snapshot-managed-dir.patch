diff --git a/src/agents/agent-command.ts b/src/agents/agent-command.ts
index bdf999f..545ccd5 100644
--- a/src/agents/agent-command.ts
+++ b/src/agents/agent-command.ts
@@ -1,3 +1,4 @@
+import path from "node:path";
 import { getAcpSessionManager } from "../acp/control-plane/manager.js";
 import { resolveAcpAgentPolicyError, resolveAcpDispatchPolicyError } from "../acp/policy.js";
 import { toAcpRuntimeError } from "../acp/runtime/errors.js";
@@ -22,6 +23,7 @@ import {
 } from "../config/config.js";
 import { resolveAgentIdFromSessionKey, type SessionEntry } from "../config/sessions.js";
 import { resolveSessionTranscriptFile } from "../config/sessions/transcript.js";
+import { resolveStateDir } from "../config/paths.js";
 import {
   clearAgentRunContext,
   emitAgentEvent,
@@ -85,6 +87,7 @@ import {
 import { buildWorkspaceSkillSnapshot } from "./skills.js";
 import { matchesSkillFilter } from "./skills/filter.js";
 import { getSkillsSnapshotVersion, shouldRefreshSnapshotForVersion } from "./skills/refresh.js";
+import { resolveBundledSkillsDir } from "./skills/bundled-dir.js";
 import { normalizeSpawnedRunMetadata } from "./spawned-context.js";
 import { resolveAgentTimeoutMs } from "./timeout.js";
 import { ensureAgentWorkspace } from "./workspace.js";
@@ -522,9 +525,15 @@ async function agentCommandInternal(
       shouldRefreshSnapshotForVersion(currentSkillsSnapshot.version, skillsSnapshotVersion) ||
       !matchesSkillFilter(currentSkillsSnapshot.skillFilter, skillFilter);
     const needsSkillsSnapshot = isNewSession || shouldRefreshSkillsSnapshot;
+    // Resolve managed and bundled skills directories explicitly to ensure correct paths
+    // are used regardless of module-level CONFIG_DIR evaluation timing.
+    const managedSkillsDir = path.join(resolveStateDir(), "skills");
+    const bundledSkillsDir = resolveBundledSkillsDir();
     const skillsSnapshot = needsSkillsSnapshot
       ? buildWorkspaceSkillSnapshot(workspaceDir, {
           config: cfg,
+          managedSkillsDir,
+          bundledSkillsDir,
           eligibility: {
             remote: getRemoteSkillEligibility({
               advertiseExecNode: canExecRequestNode({
diff --git a/src/auto-reply/reply/commands-system-prompt.ts b/src/auto-reply/reply/commands-system-prompt.ts
index 09f4b1f..6d3bcbe 100644
--- a/src/auto-reply/reply/commands-system-prompt.ts
+++ b/src/auto-reply/reply/commands-system-prompt.ts
@@ -1,4 +1,5 @@
 import type { AgentTool } from "@mariozechner/pi-agent-core";
+import path from "node:path";
 import { resolveSessionAgentIds } from "../../agents/agent-scope.js";
 import { resolveBootstrapContextForRun } from "../../agents/bootstrap-files.js";
 import { canExecRequestNode } from "../../agents/exec-defaults.js";
@@ -9,9 +10,11 @@ import { createOpenClawCodingTools } from "../../agents/pi-tools.js";
 import { resolveSandboxRuntimeStatus } from "../../agents/sandbox.js";
 import { buildWorkspaceSkillSnapshot } from "../../agents/skills.js";
 import { getSkillsSnapshotVersion } from "../../agents/skills/refresh.js";
+import { resolveBundledSkillsDir } from "../../agents/skills/bundled-dir.js";
 import { buildSystemPromptParams } from "../../agents/system-prompt-params.js";
 import { buildAgentSystemPrompt } from "../../agents/system-prompt.js";
 import type { WorkspaceBootstrapFile } from "../../agents/workspace.js";
+import { resolveStateDir } from "../../config/paths.js";
 import { getRemoteSkillEligibility } from "../../infra/skills-remote.js";
 import { buildTtsSystemPromptHint } from "../../tts/tts.js";
 import type { HandleCommandsParams } from "./commands-types.js";
@@ -45,10 +48,16 @@ export async function resolveCommandsSystemPromptBundle(
     cfg: params.cfg,
     sessionKey: params.sessionKey ?? params.ctx.SessionKey,
   });
+  // Resolve managed and bundled skills directories explicitly to ensure correct paths
+  // are used regardless of module-level CONFIG_DIR evaluation timing.
+  const managedSkillsDir = path.join(resolveStateDir(), "skills");
+  const bundledSkillsDir = resolveBundledSkillsDir();
   const skillsSnapshot = (() => {
     try {
       return buildWorkspaceSkillSnapshot(workspaceDir, {
         config: params.cfg,
+        managedSkillsDir,
+        bundledSkillsDir,
         agentId: sessionAgentId,
         eligibility: {
           remote: getRemoteSkillEligibility({
diff --git a/src/auto-reply/reply/session-updates.ts b/src/auto-reply/reply/session-updates.ts
index b930a67..4ee6243 100644
--- a/src/auto-reply/reply/session-updates.ts
+++ b/src/auto-reply/reply/session-updates.ts
@@ -5,6 +5,7 @@ import { resolveSessionAgentId } from "../../agents/agent-scope.js";
 import { canExecRequestNode } from "../../agents/exec-defaults.js";
 import { buildWorkspaceSkillSnapshot } from "../../agents/skills.js";
 import { matchesSkillFilter } from "../../agents/skills/filter.js";
+import { resolveBundledSkillsDir } from "../../agents/skills/bundled-dir.js";
 import {
   ensureSkillsWatcher,
   getSkillsSnapshotVersion,
@@ -17,6 +18,7 @@ import {
   updateSessionStore,
 } from "../../config/sessions.js";
 import type { OpenClawConfig } from "../../config/types.openclaw.js";
+import { resolveStateDir } from "../../config/paths.js";
 import { resolveStableSessionEndTranscript } from "../../gateway/session-transcript-files.fs.js";
 import { logVerbose } from "../../globals.js";
 import { getRemoteSkillEligibility } from "../../infra/skills-remote.js";
@@ -148,9 +150,15 @@ export async function ensureSkillSnapshot(params: {
   const shouldRefreshSnapshot =
     shouldRefreshSnapshotForVersion(existingSnapshot?.version, snapshotVersion) ||
     !matchesSkillFilter(existingSnapshot?.skillFilter, skillFilter);
+  // Resolve managed and bundled skills directories explicitly to ensure correct paths
+  // are used regardless of module-level CONFIG_DIR evaluation timing.
+  const managedSkillsDir = path.join(resolveStateDir(), "skills");
+  const bundledSkillsDir = resolveBundledSkillsDir();
   const buildSnapshot = () =>
     buildWorkspaceSkillSnapshot(workspaceDir, {
       config: cfg,
+      managedSkillsDir,
+      bundledSkillsDir,
       agentId: sessionAgentId,
       skillFilter,
       eligibility: { remote: remoteEligibility },
diff --git a/src/cron/isolated-agent/skills-snapshot.ts b/src/cron/isolated-agent/skills-snapshot.ts
index c66825c..16ee8a5 100644
--- a/src/cron/isolated-agent/skills-snapshot.ts
+++ b/src/cron/isolated-agent/skills-snapshot.ts
@@ -1,7 +1,10 @@
+import path from "node:path";
 import { canExecRequestNode } from "../../agents/exec-defaults.js";
 import type { SkillSnapshot } from "../../agents/skills.js";
 import { matchesSkillFilter } from "../../agents/skills/filter.js";
+import { resolveBundledSkillsDir } from "../../agents/skills/bundled-dir.js";
 import type { OpenClawConfig } from "../../config/types.openclaw.js";
+import { resolveStateDir } from "../../config/paths.js";
 import {
   buildWorkspaceSkillSnapshot,
   getRemoteSkillEligibility,
@@ -32,8 +35,14 @@ export function resolveCronSkillsSnapshot(params: {
     return existingSnapshot;
   }
 
+  // Resolve managed and bundled skills directories explicitly to ensure correct paths
+  // are used regardless of module-level CONFIG_DIR evaluation timing.
+  const managedSkillsDir = path.join(resolveStateDir(), "skills");
+  const bundledSkillsDir = resolveBundledSkillsDir();
   return buildWorkspaceSkillSnapshot(params.workspaceDir, {
     config: params.config,
+    managedSkillsDir,
+    bundledSkillsDir,
     agentId: params.agentId,
     skillFilter,
     eligibility: {
