diff --git a/scripts/release-check.ts b/scripts/release-check.ts
index 86b2158..fef379d 100755
--- a/scripts/release-check.ts
+++ b/scripts/release-check.ts
@@ -123,6 +123,7 @@ function runPackDry(): PackResult[] {
     encoding: "utf8",
     stdio: ["ignore", "pipe", "pipe"],
     maxBuffer: 1024 * 1024 * 100,
+    shell: true,
   });
   return JSON.parse(raw) as PackResult[];
 }
@@ -135,6 +136,7 @@ function runPack(packDestination: string): PackResult[] {
       encoding: "utf8",
       stdio: ["ignore", "pipe", "pipe"],
       maxBuffer: 1024 * 1024 * 100,
+      shell: true,
     },
   );
   return JSON.parse(raw) as PackResult[];
@@ -169,6 +171,7 @@ function installPackedTarball(prefixDir: string, tarballPath: string, cwd: strin
       cwd,
       encoding: "utf8",
       stdio: "inherit",
+      shell: true,
     },
   );
 }
@@ -178,6 +181,7 @@ function resolveGlobalRoot(prefixDir: string, cwd: string): string {
     cwd,
     encoding: "utf8",
     stdio: ["ignore", "pipe", "pipe"],
+    shell: true,
   }).trim();
 }
 
