'use strict';

const path = require('path');
const esbuild = require('esbuild');
const repoRoot = path.resolve(__dirname, '../..');

esbuild.buildSync({
  entryPoints: [
    path.join(repoRoot, 'src/renderer/libs/openclaw-chat/model/browser-extension-stream.ts'),
  ],
  outfile: path.join(
    repoRoot,
    'resources/browser-extension/conversation-overlay/modules/sidepanel-stream.js',
  ),
  bundle: true,
  format: 'esm',
  platform: 'browser',
  target: ['chrome125'],
  tsconfig: path.join(repoRoot, 'tsconfig.json'),
  banner: { js: '// Generated by scripts/browser/build-browser-extension-stream.cjs. Do not edit.' },
});
