# OpenClaw 2026.9.6 内网功能接入计划

日期：2026-09-25。状态：技能整理回归修复已提交；主助手历史学习与提案审核已实现，真实内网模型端到端待验。
浏览器介入和会话归档仍未实施，不属于本次技能功能引入范围。

## 目标与用户已确认的约束

在已经完成的 OpenClaw v2026.9.6 升级基础上，将适合公司内网使用的能力接入现有产品界面。
本轮按顺序实施三项：**技能提炼与审核 → 浏览器实时查看与人工介入 → 会话归档与存储管理**。
先完成每项的最小完整流程及验证，再推进下一项。

- 软件部署在公司内网。除浏览器可以访问公司外网外，应用、Gateway 和其他执行组件只能连接公司内网服务。
- 模型调用复用现有公司内网模型配置；不新增公网模型、外部账号或在线下载的运行时依赖。
- 浏览器可访问外网，不代表应用后台有公网访问能力，也不代表后台拥有浏览器中的登录身份。
- 公司 MCP 涉及内部身份与业务权限，没有确定的客户端接入规范；本轮不做 MCP 接入、登录或诊断改造。
- 不提取浏览器 Cookie、令牌或证书来替代正式的服务认证，不把浏览器作为后台请求的通用公网代理。
- 本计划约束新增功能的网络依赖，不授权顺带重构整个软件的网络策略或移除既有插件。

## 基线与阅读顺序

1. 阅读根目录 `AGENTS.md`，并遵守相关目录下的补充约定。
2. 阅读 [升级审计](openclaw-upgrade-v2026.9.6.md) 与 [补丁清单](../../scripts/patches/v2026.9.6/README.md)。
3. 阅读 [浏览器设计](browser-settings-design.md)、[助手设计](multi-agent.md)，以及相关架构文档。
4. 对照同级 `../openclaw` 的 **v2026.9.6 标签**检查协议和实现，不以最新工作分支推断已发布能力。
   发布记录位于上游 `CHANGELOG/2026.9.3.md` 至 `2026.9.6.md`。

升级提交为 `bcc6e493b`（`feat(engine): upgrade OpenClaw to v2026.9.6`）。实施前检查当前分支、工作区和后续变更，不重做已经完成的升级。

升级已包含进度卡刷新、浏览器扩展基线 2.3.0、QuickJS/GitHub 原生插件打包及 ACP 依赖更新。
这些不等于本计划中的产品流程已经完成。尤其不要把原生 Web Control UI 的界面能力视为 Electron 已接入能力。

## 阶段 0：核实原生接口与产品缺口

- [ ] 检查三项功能在本软件中的已有入口、数据流和测试，复用现有实现。
- [ ] 从 v2026.9.6 源码确认实际 RPC、事件、参数、权限范围、错误和生命周期；在本文件补充真实接口映射与源码位置。
- [ ] 检查锁定并打包后的 Gateway 是否真正注册这些接口；“上游有源码”不等于“当前产物可调用”。
- [ ] 核实各功能只依赖本地组件、内网模型以及已配对的浏览器，不要求公网插件或在线安装。
- [ ] 区分可直接接入、需要应用适配、原生能力缺失三种情况，不杜撰接口或用空界面宣称完成。

本阶段是实现工作的一部分，无需仅为了开始读代码或普通适配再次请求用户确认。
若某项依赖当前不存在的原生能力或企业授权，记录具体证据、可完成范围和阻塞点，继续其他独立项；不得绕过权限或悄悄替换目标。

## 阶段 1：技能提炼与审核

### 2026-09-25 回归修复与手动学习评估

整理任务聚合修复已提交为 `8f6ee034c`。后续手动学习与审核实现情况见下节。
源码核实对象为同级仓库的 `v2026.9.6` 标签，未修改上游或运行时补丁。

- 根因：Main 已把 declaration job 标为 managed，但丢弃 `declarationKey`；Renderer 仅按旧
  `skillCollectionReview` payload 判断，无法识别新版 `agentTurn`，导致各 Agent 分别显示。
- 修复：Main → Shared → Renderer 保留原生 declaration key，按 `skill-collection-review:<agentId>`
  聚合；保留真实执行 payload 和各成员 job ID，复用统一开关、详情、历史及助手名称展示。
- 可关闭但不能通过 cron 删除：`src/cron/service/ops-mutations.ts` 拒绝删除 system-owned monitor；
  `src/cron/skill-collection-review-monitor.ts` 在 mode 非 auto 时继续投影禁用任务。
  现有应用配置默认显式写入 off，保留用户显式 auto/propose，开关仍通过原生 config.patch 控制。
- 手动学习建议引入，但应与审核一起交付。原生 UI 的
  `ui/src/pages/skill-workshop/skill-workshop-page.ts` 通过 `sessions.create`
  传入 `agentId`、`displayName`、`message`、`idempotencyKey` 创建学习会话，并处理 initialRun。
  `learning-prompt.ts` 要求读取该 Agent 可访问的历史，自行选择相关会话；没有任意勾选会话的参数。
- 手动触发本身不修改自动学习配置；它遵循当前 Workshop 模式，Auto 可能直接修改技能，
  不能把“手动触发”等同于“必经审核”。`src/skills/workshop/config.ts` 的原生
  `approvalPolicy` 默认也是 auto；接入时须核实并设置真正的提案审核策略，不能只改按钮文案。
- 审核接口位于 `src/gateway/server-methods/skills.ts`：`skills.proposals.list`、
  `skills.proposals.inspect`、`skills.proposals.apply`、`skills.proposals.reject`。
  审核写入携带所属 Agent、proposalId 和 expectedRevisionHash，防止批准旧版本。
  `skills-proposal-history.ts` 已退役历史批量扫描，不应调用旧扫描接口代替学习会话。
- 本次验证：lint、build 通过；直接运行相关 Vitest 测试 4 文件 / 69 用例通过。
  npm test 包装器在测试开始前被 Windows 锁定的 better_sqlite3.node 阻止重建，
  Electron ABI 检查确认仍兼容；未执行全量测试或真实内网模型端到端学习。

### 2026-09-25 手动学习与审核实现

- 学习直接在现有聊天中输入原生 `/learn`，使用当前会话的权限、停止与继续机制；
  没有独立学习启动流程、学习方向表单或启动前策略修改。是否产生提案遵循原生能力与配置。
- 插件 → 技能中的“技能提案”按钮位于“导入技能”右侧，打开独立模态弹窗，
  仅查看和审核主助手 main 所属提案；不提供其他助手或逐会话筛选。
  使用帮助默认折叠，说明技能草稿、批准作用及操作方式。
- 通过原生 skills.proposals.list/inspect/apply/reject 管理提案；更新提案并列显示完整当前指令
  和提案指令，展示完整支持文件与扫描发现。通过 skills.workshop.read 读取当前指令并核对目标哈希。
- 审核绑定 agentId、proposalId、expectedRevisionHash；重复点击互斥，过期、冲突或响应不确定时
  清除旧审核快照并重新读取状态，不自动重放写操作。缺失草稿无法批准。
- 应用后刷新 main 的 skills.status，openclaw-workshop 来源显示为“助手提炼的技能”，
  不走用户导入目录删除流程。技能写入原生主助手独立技能目录，可跨工程使用，不写入当前工程。
  没有新增持久化提案表、应用自建技能目录或正文缓存。
- 弹窗打开后才读取提案，每 15 秒及窗口重新聚焦时刷新；关闭/卸载释放定时器与 focus 监听。
  写入期间禁止关闭，切换状态筛选清除旧详情；重新打开从原生恢复状态。
- 审核入口不创建周期任务，不修改自主学习或发布策略，与原有技能库自动整理卡片相互独立。
- 已核对锁定 v2026.9.6 源码及当前打包运行时的原生提案、技能读取和 learn handler，
  没有改上游源码、运行时补丁或打包裁剪。
- 已执行提案服务、审核界面、技能来源管理、技能状态查询、翻译及定时任务聚合的聚焦测试，
  并完成 lint、build、Electron TypeScript 检查。
  标准 npm test 的前置 better-sqlite3 重建此前被 Windows 文件锁阻断；
  Electron Node 的全套替代运行存在环境相关失败，不能等同于标准全量测试通过。
  真实公司模型、历史权限与学习 → 审核 → 生效闭环仍待实际环境验收。

### 与现有定时任务的关系（2026-09-25 补充）

用户指出已有多条从历史任务学习/整理技能的定时任务。因此，本阶段应定位为**现有 Workshop 的入口与审核补齐**，不是新建第二套技能学习系统，也不是新增周期扫描任务。

已核实代码：

- 应用 `src/renderer/features/scheduled-tasks/components/skillReviewCard.ts` 将识别到的系统技能审查任务聚合展示；现有文案为“技能库自动整理”，描述每周合并重复技能、清理低质量条目。
- v2026.9.6 上游 `src/cron/skill-collection-review-monitor.ts` 按 Agent 投影每周任务，是否启用取决于 Workshop `autonomous.mode === 'auto'` 和执行能力。多条任务可能对应不同 Agent，不应直接视为重复任务并删除。
- 上游 `src/skills/workshop/maintenance-prompt.ts` 指示审阅已有 Workshop 文件、合并、精简和修正引用；这与主动从历史会话学习的触发和输入不同，但使用同一技能体系。
- 9.6 周任务实际使用 `agentTurn` 和系统 declaration key；本次已贯通 Main → Shared → Renderer 的 declarationKey 映射并修复识别。

实施前先只读核对实际任务的系统归属、Agent、payload、调度和结果；用户自行创建的历史提炼任务可能另有 prompt，不能一概当成原生整理任务。不改动或停用这些任务，不输出含用户内容的原始记录。

- [ ] 先确认已有学习任务是否已覆盖手动运行、结果查看和审核；已覆盖的入口直接复用。
- [x] 修复 9.6 系统任务识别缺口，按原生权威归属映射，不依赖可编辑的任务名称判定。
- [ ] 手动与定时学习复用同一技能归属、提案来源和审核流程；不新建周期任务或独立技能目录。
- [ ] 若已有能力已满足下述流程，只补缺口和测试，并记录复用范围，避免为计划条目重复开发。

目标：用户主动从过去的工作经验提炼技能，查看完整修改内容，审核后再生效。
上游依据：9.3 的 agent-owned Workshop 与完整指令比较，9.4 的“从历史会话学习”及提案流程。

### 最小交付

- [x] 技能页仅保留“技能提案”按钮，独立弹窗查看与审核，关闭后停止轮询。
- [x] 学习直接在现有聊天中使用原生 `/learn`，不再另建学习启动流程。
- [x] 明确提案列表属于主助手 main，不制作逐会话筛选。
- [x] 学习使用原有聊天的权限、停止和继续体验；审核入口不创建会话。
- [x] 展示原生提案状态，完整并列比较当前指令与提案指令。
- [x] 提供批准与拒绝，操作后重新获取权威状态；处理过期、冲突和草稿缺失。
- [x] 批准后刷新原生有效技能清单；不另建技能元数据来源。
- [x] 审核入口不修改学习或发布策略；原生 `/learn` 遵循已有配置。

### 实现边界

优先检查 `src/main/plugins/skills/`、`src/renderer/features/plugins/skills/`、
`src/main/openclaw/config/` 和现有聊天 Gateway 客户端。
Workshop 集合属于 Agent，不按不同项目重复生成副本，也不写入应用自带只读技能资源目录。
历史读取、提炼执行和提案持久化由 OpenClaw 负责；Main/Redux 不建立正文缓存或平行提案数据库。

### 验收

- [ ] 内网模型可用时，发起学习 → 查看提案 → 审核 → 技能列表刷新形成完整流程。
- [ ] 拒绝提案不会改动已安装技能；重复点击不会重复应用提案。
- [ ] 模型不可用、Gateway 断连、任务停止、草稿缺失、审核冲突均有可理解且可恢复的状态。
- [ ] 重连、切换会话和重启后可恢复原生已有任务/提案；不会自动重放写操作。
- [ ] 不跨 Agent 错读或错写技能；不将单次请求默认为永久指令。

## 阶段 2：浏览器实时查看与人工介入

目标：用户能够知道 Agent 在哪个页面工作，查看页面变化，并在需要时介入后继续任务。
上游依据：9.3 的浏览器实时页面与截图回退、9.5 的浏览器协作改进。

### 最小交付

- [ ] 复用现有浏览器设置、扩展配对和页面/任务关联，在聊天侧提供页面查看入口。
- [ ] 明确区分未配对、连接中、已连接、页面不可用和连接中断；不要把旧截图当作实时画面。
- [ ] 若当前平台和原生接口支持画面流，接入实时预览；不支持时使用有刷新时间和状态的截图预览。
- [ ] 核实原生是否提供控制权交接协议：有则沿用；没有则优先采用“暂停相关浏览器操作 → 用户在真实浏览器操作 → 用户明确继续”的流程。
- [ ] 暂停必须有执行层确认，不能只改变按钮文案。如果无法可靠暂停相关操作，不提供名为“接管”的假入口，并记录这一限制。
- [ ] 人工介入期间避免 Agent 与用户争抢同一页面；继续前重新校验连接、标签页和执行状态。
- [ ] 用户登录和完成企业验证仍在真实浏览器中进行，应用不收集登录凭据。
- [ ] 隐藏预览、切换会话或窗口关闭时释放订阅/流；不影响其他会话的浏览器任务。

### 实现边界

优先检查 `src/renderer/features/settings/browser/`、`src/shared/browser/`、现有聊天预览组件，
并按 `browser-settings-design.md` 查找主进程服务和扩展构建入口。
OpenClaw 所有的 `openclaw/` 配对/relay 基线与 `conversation-overlay/` 侧栏聊天继续分离。
原生 `ai.openclaw.browser_bootstrap` 与应用侧栏 native host 不得混用；保留手动配对路径。
不移植仅适用于 macOS WebKit 的能力，不引入第二套浏览器自动化执行引擎。

### 验收

- [ ] Windows 下已配对浏览器的标签页可查看，页面关闭、断线、重连均能正确更新状态。
- [ ] 流不可用时有明确的截图回退；预览停止后不会持续高频后台轮询。
- [ ] 人工介入与继续遵循真实执行状态，不出现已暂停却仍有自动化操作的情况。
- [ ] 切换会话不会串用标签页、画面或控制状态。
- [ ] 不依赖应用后台访问外网，不读取或转存浏览器身份凭据。

## 阶段 3：会话归档与存储管理

目标：让用户查看历史占用，并通过原生冷归档减少长期存储压力，仍能恢复历史。
上游依据：9.5 的冷归档与存储统计、9.6 的归档入口改进。

### 最小交付

- [ ] 在适合的现有设置区域展示原生可提供的存储统计、归档策略和操作状态；不估算并冒充精确占用。
- [ ] 提供原生支持的“立即归档”操作，清楚说明它作用于哪些符合条件的会话。
- [ ] 提供原生支持的自动归档开关和期限配置，默认不自动开启，保留已有显式设置。
- [ ] 原生支持单会话归档时再接入会话菜单；不以全局归档接口伪装单会话操作。
- [ ] 打开归档会话时呈现恢复状态，并通过 Gateway 重新读取历史；恢复后可以继续使用。
- [ ] 遵循原生对活跃任务、不符合条件会话、并发归档/恢复的限制。
- [ ] 明确“冷归档”“从列表隐藏”“删除”不同，不借归档执行删除或清空产品会话映射。
- [ ] 检查应用已有备份/导出流程是否涵盖原生归档文件；存在缺口时修正受影响流程或明确其真实覆盖范围。

### 实现边界

优先检查 `src/main/openclaw/sessions/`、`src/main/data/coworkStore.ts`、
`src/renderer/features/cowork/components/sessions/` 和设置区域。
压缩、索引、恢复和归档文件归属由 OpenClaw 管理，不直接改写原生数据库，不在应用中再写一套压缩存储。
保留本软件的会话身份、分组和历史映射。归档不等于历史永久可搜索，按原生恢复/索引状态展示真实行为。

### 验收

- [ ] 使用隔离测试历史完成统计 → 归档 → 重启 → 打开并恢复 → 继续会话。
- [ ] 文本、原有附件引用和会话身份可按原生语义恢复；磁盘占用读数来自实际原生统计。
- [ ] 活跃任务不会被错误归档；失败、磁盘空间不足、断连和恢复中断不会误报成功。
- [ ] 归档操作不会触发应用删除流程，也不会破坏分组、助手历史或原生历史所有权。
- [ ] 关闭自动归档后，已归档历史仍能按原生能力恢复。

## 本轮明确不做

- GitHub 阅读面板、外部 OAuth 登录、提供方多账号登录管理。
- 内网 MCP 的新增接入、认证、权限或诊断改造；现有能力保持原有职责。
- CLI Agent 选择器：尚未验证各 Agent 在公司环境中的模型、认证和运行依赖。
- 公网会话分享、远程云机器、FaceTime、会议实时语音、移动端、TypeSafe/ONNX 决策服务。
- 专家团队创建向导：作为后续候选，本轮不扩展现有 `agent-team` 或默认启用团队协作。
- 引擎自行更新机制、第二套 Control UI、与三项交付无关的基础设施重构。

## 通用实现与验证要求

- 遵循 Renderer/Main/Shared 边界，复用现有 Gateway 连接与订阅管理。确需新增 IPC 时完整更新类型、通道、handler、preload 和 Renderer 声明。
- 所有新增用户文本补齐中文和英文；使用产品元数据，不硬编码品牌。
- 写操作处理重复点击、超时后结果不确定和重连后的状态核对；不能用盲目重试造成重复执行。
- 功能不可用时展示原因，不能仅凭版本号启用一个调用必然失败的入口。
- 更新相应架构文档，至少核对 `07-plugin-system`、`10-data-storage`、`15-chat-rendering`；完成后回填本计划实际范围、接口和验证结果。
- 为真实风险编写行为测试：所属 Agent、审核写入、订阅释放、控制权状态、归档恢复、配置保持等；避免仅镜像实现的测试。
- 运行受影响测试；交付前完成 `npm run lint`、`npm run build`、`npm test`。按仓库要求恢复 Electron 原生模块 ABI。
- 如修改运行时补丁或裁剪流程，从锁定 pristine 包重建，并运行 patch proof 和 Gateway smoke；不得就地升级含历史补丁的 runtime。
- 测试使用隔离 HOME/状态/配置/工作区，不拿用户真实历史做破坏性归档验证。
- 没有可用公司模型、企业网页或真实账号时，完成可运行的自动测试与本地验证，列出真实环境待验项目，不能宣称端到端已通过。

## 新会话启动指令

可直接把以下内容发给新会话：

> 请阅读并实施 `docs/features/openclaw-intranet-feature-integration-plan.md`。
> OpenClaw 已升级到 v2026.9.6，源码在 `../openclaw`。先核实发布标签的实际接口和现有实现，
> 再依次完成技能提炼与审核、浏览器实时查看与人工介入、会话归档与存储管理。
> 本软件运行在公司内网，只有浏览器可以访问外网；其他组件只使用内网服务。
> 不做 GitHub、OAuth、MCP 权限接入或 CLI Agent 选择器，不默认启用自动学习与自动归档。
> 按计划直接开展实现和验证，更新文档与完成状态；遇到真实阻塞记录证据，并继续其他可独立完成的部分。
