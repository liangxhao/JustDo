{
  "id": "action-approval",
  "name": "Action Approval",
  "description": "Approval handling for file and scheduled-task mutation tools.",
  "contracts": {
    "trustedToolPolicies": ["core-action-approval"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "mode": {
        "type": "string",
        "enum": ["ask", "auto", "full"]
      },
      "fullAgentIds": {
        "type": "array",
        "items": { "type": "string", "minLength": 1 },
        "uniqueItems": true
      }
    }
  }
}
